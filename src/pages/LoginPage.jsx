import DocTitle from '../components/DocTitle/DocTitle';
import LoginForm from '../components/LoginForm/LoginForm';

const LoginPage = () => {
  return (
    <div>
      <DocTitle>Please, log in!</DocTitle>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
